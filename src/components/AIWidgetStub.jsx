import React from 'react'
import styles from '../styles/AIWidgetStub.module.css'

// Stub para futuro widget de Agente de IA
export default function AIWidgetStub() {
  return (
    <div className={styles.stub} aria-live="polite" aria-atomic="true">
      <button className={styles.button} type="button" aria-label="Abrir assistente de IA">
        Assistente de IA (Em breve)
      </button>
    </div>
  )
}
